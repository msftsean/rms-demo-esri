-- Enable PostGIS extension for spatial types
CREATE EXTENSION IF NOT EXISTS postgis;

-- Optional: seed small demo data (table will be created by EF on first run)
-- You can add seed inserts after EF has created the table if needed.
