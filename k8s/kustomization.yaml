apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: rms

# Production-ready configuration using Azure-compliant images
# For development, use: kubectl apply -k k8s/dev/

resources:
  - namespace.yaml
  - secret-sample.yaml
  - overlays/azure/azure-deployment.yaml
  - ingress.yaml

commonLabels:
  app: rms-demo
  version: v1.1.0
